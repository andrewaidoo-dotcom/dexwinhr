# DexwinHR — Project notes

## Layout conventions
- On all form screens/tabs, the form **title and subtext must scroll together with the rest of the form** — do not pin/sticky them. The whole form (title + subtext + fields) scrolls as one.
